name: camera_pas
description: "Camera package as a service"
version: 0.0.1
homepage: ''
publish_to: none

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  camera: ^0.11.0+1
  flutter:
    sdk: flutter
  logger: ^2.4.0
  path: ^1.9.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

